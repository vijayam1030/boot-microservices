server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        bootstrap: true
        git:
          uri: https://github.com/vijayam1030/config-server
          clone-on-start: true